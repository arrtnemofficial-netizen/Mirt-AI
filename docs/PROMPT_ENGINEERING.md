# 📋 MIRT AI - Prompt Engineering Guide

## ✅ ЩО РЕАЛІЗОВАНО

### 1. Валідація повідомлень (КРИТИЧНО!)

```python
from src.core.message_validator import validate_incoming_message

# Перевірка ДО обробки промпту
validation = validate_incoming_message(text, attachments)

if not validation.is_valid:
    # Автоматичний вихід
    return exit_with_condition(validation.exit_condition)
```

**Правила що перевіряються:**
- ✅ Порожнє повідомлення → EXIT
- ✅ Повідомлення з медіа → EXIT  
- ✅ Повідомлення з посиланням → EXIT
- ✅ Незрозумілі символи → EXIT

**Exit condition**: `"Незрозуміле повідомлення або незрозумілий формат повідомлення"`

---

### 2. Структуровані правила

| Файл                     | Що містить          |
| ------------------------ | ------------------- |
| `mirt_full_rules.yaml`   | Всі правила діалогу |
| `catalog_models.yaml`    | Каталог моделей     |
| `few_shot_examples.yaml` | Приклади JSON       |
| `prompt_variants.yaml`   | A/B тести           |

---

### 3. Критичні правила з промпту

#### 🔒 Розмірна сітка

```yaml
SIZE_RULES:
  110-120 см → 110-116
  121-131 см → 122-128
  132-143 см → 134-140
  144-155 см → 146-152
  156-168 см → 158-164

BOUNDARY_RULE:
  Зріст 120 → 110/116 (НЕ 122/128!)
  Зріст 131 → 122/128 (НЕ 134/140!)
  # На межі = менший розмір!
```

#### 📦 Процес замовлення (4 кроки)

```
Крок 1: Підтвердження моделі/розміру/кольору
  ↓ Клієнт: "Так, хочу замовити"
  
Крок 2: Запит даних доставки
  - ПІБ
  - Телефон
  - Місто + відділення НП
  ↓ Клієнт надає дані
  
Крок 3: Питання про оплату
  - Передоплата 200 грн
  - Повна оплата [СУМА] грн
  ↓ Клієнт обирає
  
Крок 4: Фіналізація
  - Записати суму ЧИСЛОМ
  - EXIT: "замовлення прийнято"
```

**КРИТИЧНО:**
- ❌ НЕ пропускати кроки
- ❌ НЕ писати "далі запитаю..."
- ✅ Суму писати ЧИСЛОМ

---

### 4. Приклад ідеального діалогу

```
К: які у вас є костюми до школи

Б: Вітаю, я менеджер Ольга🤗
   Буду рада допомогти)
   
   Покажу костюми для школи:
   
   1. Костюм жилет + штани (кольори: шоколад, сірий, чорний)
   - Відео: [LINK]
   
   2. Костюм спідниця + жилет (кольори: малина, молочний...)
   - Відео: [LINK]
   
   Яка модель подобається найбільше?😊

К: покажіть з бананами

Б: Ось фото костюма з штанами-бананами:
   - Білий: [LINK]
   - Голубий: [LINK]
   
   Стильний джинсовий костюм ☺️
   
   Уточніть, будь ласка, який зріст дівчинки?
   Наші костюми повномірні 🫶

К: зріст 122

Б: Тоді вам чудово підійде розмір 122-128 - з гарним запасом.
   Розмір 122 розрахований на зріст 122-128 см 👌
   
   Який колір найбільше сподобався?

К: Білий

Б: Маємо у білому кольорі 👌
   Ці костюми швидко зникають, але ще маємо кілька)
   
   Бажаєте оформити замовлення?🤍

К: Так, а обмін є?

Б: У нас є обмін )
   Можна забрати додому приміряти (безкоштовно через НП) ☺️
   
   Щоб зарезервувати, напишіть дані:
   📌 Місто та відділення НП
   📌 ПІБ
   📌 Номер телефону

К: [надає дані]

Б: Отож, ваше замовлення:
   Костюм білий джинс із бананами розмір 122/128
   
   Варіанти оплати:
   ✔️ Повна оплата 2020 грн
   ✔️ Передоплата 200 грн, решта на НП (+ комісія)
   
   Як вам зручніше?

К: передоплата

Б: [Записує 200 у змінну]
   [EXIT: "замовлення прийнято, перехід до оплати"]
```

---

## ⚠️ КРИТИЧНІ ЗАБОРОНИ

| Заборонено                                  | Чому              |
| ------------------------------------------- | ----------------- |
| Відповідати на порожні повідомлення         | EXIT condition    |
| Відповідати на медіа                        | EXIT condition    |
| Відповідати на посилання                    | EXIT condition    |
| Формувати > 900 символів                    | Ліміт токенів     |
| Вигадувати деталі                           | Тільки з каталогу |
| Надсилати розмірну сітку без прохання       | Правило           |
| Пропонувати інший розмір якщо вже підібрано | Правило           |
| Повторно вітатись за день                   | Правило           |
| Писати майбутні кроки                       | Правило           |
| Використовувати ** markdown                 | Правило           |

---

## 🎯 ЩО ПОТРІБНО ЗРОБИТИ ДАЛІ

### Інтеграція в систему:

1. **Додати валідацію в webhooks**
   ```python
   # src/server/main.py - Telegram/ManyChat webhooks
   
   @app.post("/webhooks/telegram")
   async def telegram_webhook(update: dict):
       text = extract_text(update)
       attachments = extract_attachments(update)
       
       # КРИТИЧНО: Перевірка ДО обробки
       validation = validate_incoming_message(text, attachments)
       if not validation.is_valid:
           return handle_exit_condition(validation.exit_condition)
       
       # Далі обробка...
   ```

2. **Додати exit conditions handler**
   ```python
   # src/core/exit_handler.py
   
   def handle_exit_condition(condition: str, session_id: str):
       """Handle exit conditions (escalation to human)."""
       # Логіка ескалації
       # Запис у БД
       # Повідомлення адміну
   ```

3. **Інтегрувати повний промпт**
   ```python
   # src/core/prompt_loader.py
   
   def load_mirt_full_prompt() -> str:
       """Load full MIRT prompt with all rules."""
       rules = load_yaml("mirt_full_rules.yaml")
       catalog = load_yaml("catalog_models.yaml")
       examples = load_yaml("few_shot_examples.yaml")
       
       return build_system_prompt(rules, catalog, examples)
   ```

4. **Тести для всіх правил**
   - Тест порожніх повідомлень ✅
   - Тест медіа ✅
   - Тест посилань ✅
   - Тест процесу замовлення ⏳
   - Тест розмірної сітки ⏳
   - Тест exit conditions ⏳

---

## 📊 Поточний статус

| Компонент           | Статус            |
| ------------------- | ----------------- |
| Message validation  | ✅ DONE (24 tests) |
| Output parser       | ✅ DONE (18 tests) |
| State validator     | ✅ DONE (21 tests) |
| A/B testing         | ✅ DONE            |
| Full rules YAML     | ✅ DONE            |
| Catalog YAML        | ✅ DONE (частково) |
| Webhook integration | ⏳ TODO            |
| Exit handler        | ⏳ TODO            |
| Order process tests | ⏳ TODO            |

---

## 🚀 Наступні кроки

1. ✅ Створити повні правила (mirt_full_rules.yaml)
2. ✅ Створити каталог моделей (catalog_models.yaml)
3. ⏳ Інтегрувати валідацію в webhooks
4. ⏳ Створити exit conditions handler
5. ⏳ Додати тести процесу замовлення
6. ⏳ Запустити на реальному трафіку

---

## 💡 Рекомендації

### Оптимізація розміру промпту

Поточний промпт дуже великий (15000+ токенів). Рекомендації:

1. **Динамічне завантаження**
   - Завантажувати тільки потрібні моделі
   - Базові правила завжди
   - Каталог - тільки релевантні моделі

2. **Компресія**
   - Скоротити описи моделей
   - Видалити дублювання
   - Використати скорочення

3. **RAG підхід**
   - Базові правила в промпті
   - Каталог у vector DB
   - Пошук релевантних моделей

---

## 📝 Чеклист для промпт-інженера

- [x] Валідація порожніх повідомлень
- [x] Валідація медіа
- [x] Валідація посилань
- [x] Правила розмірної сітки
- [x] Процес замовлення (4 кроки)
- [x] Правила привітання
- [x] Заборони
- [x] FAQ відповіді
- [x] Каталог моделей
- [ ] Інтеграція в webhooks
- [ ] Exit conditions handler
- [ ] Тести процесу замовлення
- [ ] Production deployment

---

## ⚡ Швидкий старт

```bash
# Тести валідації
python -m pytest tests/test_message_validator.py -v

# Тести парсера
python -m pytest tests/test_output_parser.py -v

# Тести станів
python -m pytest tests/test_state_validator.py -v

# Всі тести
python -m pytest tests/ -v
```

---

## 📞 Підтримка

При проблемах:
1. Перевірити логи валідації
2. Перевірити exit conditions
3. Перевірити каталог моделей
4. Звернутись до цієї документації
